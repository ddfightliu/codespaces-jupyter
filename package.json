{
  "name": "codespaces-jupyter",
  "version": "1.0.0",
  "description": "A project for Jupyter integration with GitHub Codespaces",
  "main": "index.js",
  "scripts": {
    "init": "python -m venv .venv && .venv/bin/python -m pip install --upgrade pip && source .venv/bin/activate && pip install -r requirements.txt",
    "install": "pip install -r requirements.txt",
    "freeze": "pip freeze > requirements.txt",
    "activate": ".venv/scripts/activate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
